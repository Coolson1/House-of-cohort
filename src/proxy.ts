// Route protection proxy — implemented in Phase 3 (NextAuth integration).
// Placeholder: passes all requests through until auth is configured.
// Next.js 16 renamed middleware.ts → proxy.ts. Export named "proxy" or use default export.
export function proxy() {}

export const config = {
  matcher: [],
};
